import { Box } from "@mui/material"
import { MainBody } from './'

export const MarketMaker = () => {
  return (
    <Box pt={12} sx={{
      background: '#454677',
    }}>
      <MainBody />
    </Box>
  )
}
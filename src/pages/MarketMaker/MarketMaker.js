import { Box } from "@mui/material"
import { Topbar } from './'

export const MarketMaker = () => {
  return (
    <Box py={12} sx={{
      background: '#454677',
    }}>
      <Topbar />
      
    </Box>
  )
}